import "package:flutter/material.dart";

// Tema placeholder. El theming real se hará en Fase 4.
class AppTheme {
  static ThemeData light() => ThemeData(useMaterial3: true);
  static ThemeData dark() => ThemeData.dark(useMaterial3: true);
}
